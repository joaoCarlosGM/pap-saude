import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "br.com.papsaude.app",
  appName: "PAP Saúde",
  webDir: "out",

  android: {
    backgroundColor: "#ffffff",
  },
}

export default config